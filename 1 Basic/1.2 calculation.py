hrs = input("Enter Hours:")
hrs = float(hrs) # can also make it shorter as hrs = float (input("Enter hours:"))
rate = input ("Enter rate:")
rate = float(rate) # same as above
pay = hrs * rate # change the int() into float() before the calculation
print ("pay:", pay)

